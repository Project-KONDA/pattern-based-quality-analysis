name: BuildAndPushDockerImageToHarborDev

on:
  push:
    branches: [ "development2"]
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Fetch all history for tags
          
      - name: Harbor Login
        uses: docker/login-action@v3
        with:
          registry: harbor.gwdg.de
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Get Maven Project Version
        run: |
          cd qualitypatternmodel
          POM_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          BASE_VERSION=${POM_VERSION%.0}
          MASTER_PATCH=$(git rev-list v$POM_VERSION..origin/master --count --first-parent)
          BASE_COMMIT=$(git merge-base origin/master HEAD)
          DEV_PATCH=$(git rev-list $BASE_COMMIT..HEAD --count)
          VERSION="dev-$BASE_VERSION.$MASTER_PATCH.$DEV_PATCH"
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Harbor Build and Push
        uses: docker/build-push-action@v5
        with:
          context: qualitypatternmodel
          file: qualitypatternmodel/Dockerfile
          build-args: MAVEN_VERSION=${{ env.VERSION }}
          push: true
          tags: harbor.gwdg.de/aqinda/qpm:dev, harbor.gwdg.de/aqinda/qpm:current, harbor.gwdg.de/aqinda/qpm:dev-${{ env.VERSION }}
